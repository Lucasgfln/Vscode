/*
    printf(" Digite se pretende usar gasolina ou alcool (g = gasolina | a = alcool): ");
        scanf(" %c", &tc);

        tc = tolower(tc);

    if (tc != 'g' && tc != 'a'){

        while (tc != 'g' && tc != 'a') {
            printf("\n\n<=================== E R R O ! ! ===================>\n\n");
            printf(" Use *g* para gasolina e *a* para alcool: ");
            scanf(" %c", &tc);

            tc = tolower(tc);
        }

    }

    if (tc == 'g'){
        

    }
*/